services:
  - type: web
    name: roadassist-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        sync: false          # you'll paste this in Render dashboard
      - key: SECRET_KEY
        sync: false
      - key: ALGORITHM
        value: HS256
      - key: ACCESS_TOKEN_EXPIRE_MINUTES
        value: 1440
      - key: APP_ENV
        value: production
      - key: ALLOWED_ORIGINS
        sync: false          # you'll paste your Vercel URL here after deploying frontend
